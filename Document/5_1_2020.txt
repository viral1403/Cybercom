Today completed git commands. learned difference in private and public Repositories.

Found "git checkout <branch/origin>" can be used to change between branches and origin.
Added folders ,files within the Repositories.

Learned "fetch" command is used for checking the update on repository and 
shows changes to be commites.

Learned the SSH key use and requirement on creation  of repositories.

without creating a file/foolder on git project folder we cannot access the files to add to git.

Found that empty Directories can't be inserted in the Repositories.

Files can be Replaced or Overwritten on pushing them.
