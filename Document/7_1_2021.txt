learned to switch head between branches using "git checkout -b  <branch Name>"
it can transfer us to the origin and branch if only want to tranfer head to branch can 
use  "git switch <branch-name>".

learned the difference between git rebase which does not commit and update the HEAD
and  merge commiy which updates HEAD and commits.

The use of Stash which is used for reverting any file which is unecessary.

studied some of the common commands using git help.

started watching the videos on Javascript Session.